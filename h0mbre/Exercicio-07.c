#include <stdio.h>
#include <math.h>

int main(void) 
{
    float A, B, C;
    
    
    printf("Insira o valor de A: ");
    scanf("%f", &A);
    printf("Insira o valor de B: ");
    scanf("%f", &B);
    printf("Insira o valor de C: ");
    scanf("%f", &C);
    
    delta = b*b - 4*a*c
    
    x1 = (-b + sqrt(delta))/2a
    x2 = (-b - sqrt(delta))/2a
    
    

    return 0;
}
